{
  "id": "manual-already-covered",
  "skill": "agent-evolve",
  "tier": "full",
  "description": "关闭模式下的手动调用发现规则源已有同语义规则时，返回已有规则覆盖且不产生第二份规则。",
  "files": {
    "AGENTS.md": "# Project Agent Rules\n\n## Architecture\n\n- 底层解析函数或解析器遇到可预期的输入错误时，必须返回带 `ok` 字段的结构化结果，由调用者决定如何处理。\n"
  },
  "turns": [
    {
      "prompt": "$agent-evolve：AGENTS.md 是未来 agent 自动加载的唯一项目规则源。请沉淀这条反馈：底层解析函数或解析器遇到可预期的输入错误时，必须返回带 ok 字段的结构化结果，由调用者决定如何处理。",
      "checks": [
        { "type": "responseIncludes", "value": "反馈结论：已有规则覆盖" },
        { "type": "responseIncludes", "value": "原因：" },
        { "type": "responseIncludes", "value": "证据：" },
        { "type": "responseIncludes", "value": "目标位置：" },
        { "type": "responseIncludes", "value": "建议变更：不适用" }
      ]
    }
  ],
  "postChecks": [
    { "type": "fileUnchanged", "path": "AGENTS.md" },
    { "type": "trajectoryIncludes", "value": "agent-evolve" }
  ],
  "criteria": [
    "回应识别反馈与 AGENTS.md 的既有规则语义相同。",
    "回应返回已有规则覆盖、原因、证据、目标位置与建议变更：不适用。",
    "回应引用既有规则位置与查重范围，没有追加第二份规则。",
    "AGENTS.md 保持不变。"
  ]
}
