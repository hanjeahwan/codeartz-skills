{
  "id": "manual-unrouted-doc",
  "skill": "agent-evolve",
  "tier": "full",
  "description": "已有规范文档但读取入口尚未路由时，只提案权威落点与最小读取路由。",
  "files": {
    "AGENTS.md": "# Project Agent Rules\n\n- 本文件由未来 agent 自动加载。\n",
    "docs/architecture.md": "# Architecture Guide\n\n## Error Boundaries\n\n本节记录项目长期架构约束。\n"
  },
  "turns": [
    {
      "prompt": "$agent-evolve：请评估这条反馈但不要修改文件：底层解析器遇到可预期输入错误时，应把处理决定交给调用方。AGENTS.md 会被未来 agent 自动加载；docs/architecture.md 是项目维护的长期架构规范，但 AGENTS.md 目前没有路由到它。",
      "checks": [
        { "type": "responseIncludes", "value": "反馈结论：已提案" },
        { "type": "responseIncludes", "value": "docs/architecture.md" },
        { "type": "responseIncludes", "value": "AGENTS.md" },
        { "type": "responseMatches", "pattern": "(?:读取|路由)" },
        { "type": "responseIncludes", "value": "推荐操作：" }
      ]
    }
  ],
  "postChecks": [
    { "type": "fileUnchanged", "path": "AGENTS.md" },
    { "type": "fileUnchanged", "path": "docs/architecture.md" },
    { "type": "trajectoryIncludes", "value": "agent-evolve" }
  ],
  "criteria": [
    "回应把 docs/architecture.md 识别为规则权威落点，把 AGENTS.md 识别为读取入口。",
    "回应只在 docs/architecture.md 提议实际规则，在 AGENTS.md 只提议最小读取路由。",
    "回应说明新增读取路由需要用户批准，safe 不自动创建规则基础设施。",
    "两个文件都保持不变。"
  ]
}
