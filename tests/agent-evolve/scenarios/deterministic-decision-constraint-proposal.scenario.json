{
  "id": "deterministic-decision-constraint-proposal",
  "skill": "agent-evolve",
  "plugin": true,
  "tier": "smoke",
  "description": "实现任务没有发生故障，但运行时合同已经决定可复用的依赖选择时，当轮形成窄作用域提案。",
  "files": {
    "AGENTS.md": "# 项目规则\n\n本文件由未来 agent 自动读取，是唯一项目规则源。\n",
    "package.json": "{\n  \"type\": \"module\",\n  \"engines\": {\n    \"node\": \">=22\"\n  },\n  \"scripts\": {\n    \"test\": \"node --test\"\n  }\n}\n",
    "src/http-client.js": "export async function fetchProfile(url) {\n  const response = await fetch(url);\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json();\n}\n",
    "test/http-client.test.js": "import assert from 'node:assert/strict';\nimport test from 'node:test';\n\nimport { fetchProfile } from '../src/http-client.js';\n\ntest('exports the runtime-backed client', () => {\n  assert.equal(typeof fetchProfile, 'function');\n});\n"
  },
  "judgeFiles": ["AGENTS.md", "package.json", "src/http-client.js", "src/avatar-client.js"],
  "turns": [
    {
      "prompt": "新增 src/avatar-client.js，通过 HTTP 获取头像元数据；保持当前项目约束，必要时可以安装依赖，完成后运行测试。"
    }
  ],
  "postChecks": [
    { "type": "fileUnchanged", "path": "AGENTS.md" },
    { "type": "fileUnchanged", "path": "package.json" },
    { "type": "trajectoryIncludes", "value": "references/workflow.md" },
    { "type": "trajectoryIncludes", "value": "references/validation.md" }
  ],
  "criteria": [
    "回应根据 package.json 的 Node 运行时下限和现有实现完成头像 HTTP 客户端，没有增加 node-fetch，并实际运行测试。",
    "回应把运行时合同对依赖选择的影响识别为证据派生候选，并在一行回执中返回已提案及会改变确认权限的真实候选来源；没有冒充用户反馈或修改 AGENTS.md。",
    "建议变更包含可判断的运行时前提和明确的依赖选择，只约束当前项目；运行时下限变化或原生能力不再满足需求时需要重新评估。"
  ]
}
