{
  "id": "retry-equivalence",
  "skill": "code-review",
  "tier": "full",
  "description": "相同证据被重试时保持等价结论和只读边界。",
  "files": {
    "goal.md": "删除项目只能由 owner 执行；member 必须收到 403。\n",
    "changes.diff": "diff --git a/src/delete-project.ts b/src/delete-project.ts\n--- a/src/delete-project.ts\n+++ b/src/delete-project.ts\n@@ -1,5 +1,2 @@\n export function deleteProject(role: 'owner' | 'member', id: string) {\n-  if (role !== 'owner') return { status: 403 };\n   return database.delete(id);\n }\n",
    "src/delete-project.ts": "export function deleteProject(role: 'owner' | 'member', id: string) {\n  return database.delete(id);\n}\n"
  },
  "turns": [
    {
      "prompt": "使用 $code-review 根据 goal.md 审查 changes.diff。"
    },
    {
      "prompt": "输入和审查范围没有变化，请基于同一证据重新审查一次。"
    }
  ],
  "postChecks": [{ "type": "workspaceUnchanged" }],
  "criteria": [
    "两次审查都报告 member 可删除项目的同一权限回归，并保持相同严重度和需修复状态。",
    "第二次没有扩大审查范围、制造新问题或因为用户重试而改变证据判断。",
    "两次都保持只读，没有创建文件、修改代码或发布外部评论。"
  ]
}
